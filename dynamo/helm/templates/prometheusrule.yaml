apiVersion: "monitoring.coreos.com/v1"
kind: PrometheusRule
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
  labels:
    release: prometheus
spec:
  groups:
    - name: Dynamo
      rules:
        - alert: DynamoSliCritical
          expr: sum(rate(devops_playground_dynamo_requests_success[5m])) / sum(rate(devops_playground_dynamo_requests_total[5m])) < {{ .Values.prometheus.sli }}
          for: 2m
          labels:
            severity: critical
          annotations:
            summary: "Dynamo SLI is below the threshold for more than 2m minutes"
        - alert: DynamoSliWarning
          expr: sum(rate(devops_playground_dynamo_requests_success[5m])) / sum(rate(devops_playground_dynamo_requests_total[5m])) < {{ .Values.prometheus.sli }}
          for: 1m
          labels:
            severity: warning
          annotations:
            summary: "Dynamo SLI is below the threshold"
